package org.canzuo.spring.rocketmq.command;import org.apache.rocketmq.client.producer.SendResult;import org.apache.rocketmq.spring.core.RocketMQTemplate;import org.springframework.beans.factory.annotation.Value;import org.springframework.boot.CommandLineRunner;import org.springframework.messaging.Message;import org.springframework.messaging.support.MessageBuilder;import org.springframework.stereotype.Component;import javax.annotation.Resource;import java.util.ArrayList;import java.util.List;@Componentpublic class StringTopicMsgSend implements CommandLineRunner {    @Resource    private RocketMQTemplate rocketMQTemplate;    @Value("${rocketmq.topic}")    private String topicName;    @Override    public void run(String... args) throws Exception {        List<Message> msgList = new ArrayList<>();        int fractalScale = 6;        for (int i = 1; i <= fractalScale; i++) {            SendResult sendResult = rocketMQTemplate.syncSend(topicName, ""+i);            System.out.printf("syncSend to topic %s sendResult=%s %n", topicName, sendResult);        }//        for (int i = 1; i <= fractalScale; i++) {//            msgList.add(MessageBuilder.withPayload(i).build());//        }//        SendResult sendResult = rocketMQTemplate.syncSend(topicName, msgList);//        System.out.printf("syncSend to topic %s sendResult=%s %n", topicName, sendResult);    }}